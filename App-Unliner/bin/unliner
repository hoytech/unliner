#!/usr/bin/env perl

use common::sense;

use App::Unliner::Program;


my $filename = shift @ARGV
  || die "need unliner script filename as first arg";


App::Unliner::Program->new
                      ->compile_file($filename)
                      ->run( def_name => 'main', argv => \@ARGV, );
